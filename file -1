pipeline {
    agent any
    stages {
        stage('Build with Maven inside Docker') {
            steps {
                sh 'docker run --rm -v "$PWD":/app -w /app maven mvn clean install'
            }
        }
    }
}
