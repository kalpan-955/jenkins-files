pipeline {
    agent any

    stages {
        stage('clone') {
            agent {
                docker {
                    image 'maven'
                }
            }
            steps {
                sh 'mvn --version'
            }
        }

        stage('check logs') {
            steps {
                // Replace with the actual command you intend to run
                sh 'echo "filtering logs for task 3"' 
                // If `filterlogs` is a custom command, use: sh 'filterlogs task,3'
            }
        }
    }

    post {
        always {
            echo "this"
        }
    }
}
